module.exports = {
  siteUrl: "http://localhost:3000", // Ersetze mit deiner Domain
  generateRobotsTxt: true, // Erstellt zusätzlich eine robots.txt
  sitemapSize: 5000, // Maximale Anzahl von URLs pro Datei
  // exclude: ["/admin", "/dashboard"], // Seiten, die nicht in die Sitemap sollen
};
